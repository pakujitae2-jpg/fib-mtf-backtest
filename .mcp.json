{
  "mcpServers": {
    "omniroute": {
      "type": "http",
      "url": "http://localhost:20128/api/mcp/stream",
      "headers": {
        "Authorization": "Bearer ${OMNIROUTE_API_KEY}"
      }
    }
  }
}